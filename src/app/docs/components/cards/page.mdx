import {SimpleCardExample} from '@/showcase/components/cards/SimpleCard'
import PreviewCodeTabs from '@/components/CodePreview/page'

export const metadata = {
  title: 'Cards',
  description: 'Cards are used to display groups of content, photos, and other elements in a consistent format.',
}

# Cards

<div className="my-4"></div>

## Simple Card

<PreviewCodeTabs
  previewContent={
     <div className="w-full flex items-center justify-center">
       <SimpleCardExample />
    </div>
  }
codeContent={`

`}
language="jsx"
/>
